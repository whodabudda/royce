desc "copy node_modules after deploy"
task :copy_node_modules do

  on roles(:app) do |host|
     run_locally do
      execute " cd #{Dir.pwd}; rsync -a --relative node_modules #{host}:#{fetch(:release_path)} " 
    end
  end
end
